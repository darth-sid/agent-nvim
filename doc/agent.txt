*agent.nvim*  Spawn and manage AI agents in Neovim

==============================================================================
CONTENTS                                                    *agent.nvim-contents*

  1. Introduction ..................... |agent.nvim-intro|
  2. Setup ............................ |agent.nvim-setup|
  3. Commands ......................... |agent.nvim-commands|
  4. Keymaps .......................... |agent.nvim-keymaps|
  5. Configuration .................... |agent.nvim-config|
  6. Agent Manager UI ................. |agent.nvim-ui|

==============================================================================
1. INTRODUCTION                                               *agent.nvim-intro*

agent.nvim is a lightweight plugin for spawning and managing Claude/Codex AI
agents as hidden terminal buffers. It requires no external dependencies and
is fully compatible with lazy.nvim.

==============================================================================
2. SETUP                                                      *agent.nvim-setup*

Call `setup()` in your config, optionally passing overrides: >lua

  require("agent").setup({
    default_agent = "claude",
    keymaps = {
      spawn = "<leader>as",
    },
  })
<
Passing no arguments or an empty table uses all defaults.

To disable all keymaps: >lua

  require("agent").setup({ keymaps = false })
<

==============================================================================
3. COMMANDS                                                *agent.nvim-commands*

*:AgentSpawn* [type]
  Spawn a new agent. `type` is optional and defaults to `default_agent` in
  config. Accepted values: `claude`, `codex`. A name prompt opens before the
  agent is created, prefilled with the next numeric id. Agent names must be
  unique.

*:AgentList*
  Open the agent manager floating window. Lists all agents with their id,
  label, and status.

*:AgentKill* {id|name}
  Kill the agent with the given numeric `id` or exact label. Stops the
  underlying job and marks the agent as exited. Name matches only consider
  running agents.

*:AgentKillAll*
  Kill every currently running agent.

*:AgentFocus* {id|name}
  Open or jump to the terminal buffer for the agent with the given `id` or
  exact label. Name matches only consider running agents.
  If the buffer is already visible in a window, focus switches to that window.
  Otherwise the current window is reused or a new split/float is opened
  according to the `split` config option.

*:AgentRename* {id|name} [label]
  Rename the agent with the given numeric `id` or exact label. If `label` is
  omitted, a prompt is opened with the current label prefilled. Agent names
  must be unique among running agents.

If the `:AgentSpawn` prompt is cancelled, no agent is created. If the prompt is
submitted with a blank value, the next numeric id is used as the label.

==============================================================================
4. KEYMAPS                                                  *agent.nvim-keymaps*

Default keymaps (normal mode). All are overridable; set `keymaps = false` to
disable all of them.

  Key           Action
  -----------   -------------------------------------------------------
  <leader>as    :AgentSpawn (uses default agent type)
  <leader>al    :AgentList
  <leader>ak    Pick an agent via vim.ui.select and kill it
  <leader>af    Pick an agent via vim.ui.select and focus it

==============================================================================
5. CONFIGURATION                                             *agent.nvim-config*

Full config table with defaults: >lua

  require("agent").setup({
    -- Default agent type used by :AgentSpawn with no argument
    default_agent = "claude",   -- "claude" | "codex"

    -- Shell commands executed for each agent type
    commands = {
      claude = "claude",
      codex  = "codex",
    },

    -- Normal-mode keymaps. Set to false to disable all keymaps.
    keymaps = {
      spawn = "<leader>as",
      list  = "<leader>al",
      kill  = "<leader>ak",
      focus = "<leader>af",
    },

    -- How to open an agent buffer when focusing
    split = "horizontal",       -- "horizontal" | "vertical" | "float" | "current"
  })
<

==============================================================================
6. AGENT MANAGER UI                                             *agent.nvim-ui*

`:AgentList` opens a centered floating window (60 % of editor dimensions).

  Line format: >
    [id]  label       status
<

  Agent terminal buffer names include id, label, agent type, and status so
  standard statuslines show the full session info.
Keymaps inside the manager:

  <CR>    Focus the agent under cursor (closes the manager first)
  r       Rename the agent under cursor and refresh the list in place
  x       Kill the agent under cursor (refreshes the list in place)
  q       Close the manager
  <Esc>   Close the manager

==============================================================================
vim:ft=help:tw=78:ts=8:norl:
